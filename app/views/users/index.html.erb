
<h1 class="text-center mt-4">User Index</h1>
<div class="flickr_pagination mt-4">
  <%= will_paginate @users, :container => false %>
</div>

<%= render 'users_listing' %>
<div class="flickr_pagination mb-4">
  <%= will_paginate @users, :container => false %>
</div>
